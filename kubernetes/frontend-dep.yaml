apiVersion: v1
kind: deployment
metadata:
  name: frontend
  labels:
    app: SFIA2
  namespace: SFIA2-Namespace
spec:
  selector:
    matchLabels:
      app: SFIA2
  template:
    metadata:
      labels:
        app: SFIA2
spec:
  replicas: 3
  containers:
  - name: frontend
    image: aadil/frontend:latest
    ports:
    - containerPort: 5000